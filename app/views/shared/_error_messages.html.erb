<%# 同一文が複数属性に付いた場合など、full_messages の重複を除いて表示 %>
<% if model.errors.any? %>
<div class="error-alert">
  <ul>
    <% model.errors.full_messages.uniq.each do |message| %>
      <li class='error-message'><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>